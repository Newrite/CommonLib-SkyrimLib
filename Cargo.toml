[workspace]
resolver = "2"
members = [
    "core-util",
    "cstd",
    "libskyrim"
]

[profile.release]
panic = "abort"
lto = "fat"
codegen-units = 1
opt-level = "z"
strip = true

[profile.dev]
panic = "abort"
