// TODO: VERIFY - replace this opaque stand-in with the full `NiFogProperty`
// translation when non-pointer use is needed; source-backed use here is only a
// pointer field from `RE/T/TES.h`.
core_util::abstract_type! {
    pub type NiFogProperty;
}
