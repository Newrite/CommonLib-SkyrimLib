crate::core_util::abstract_type! {
    /// Source-backed forward declaration of `RE::GAllocDebugInfo`.
    ///
    /// `GMemoryHeap` currently only needs this as an opaque pointee.
    pub type GAllocDebugInfo;
}
