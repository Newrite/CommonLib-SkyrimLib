// TODO: VERIFY - replace this opaque stand-in with a fuller translation when
// `BSTempNodeManager` layout or methods are needed beyond pointer storage in
// `TES`; source: generated RTTI/VTABLE plus forward declaration in `RE/T/TES.h`.
core_util::abstract_type! {
    pub type BSTempNodeManager;
}
