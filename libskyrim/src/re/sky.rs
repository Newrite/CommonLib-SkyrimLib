// TODO: VERIFY - replace this opaque stand-in with the real `Sky` translation
// when layout or methods beyond raw pointers are needed; current `TES`
// translation only stores a pointer from `RE/T/TES.h`.
core_util::abstract_type! {
    pub type Sky;
}
