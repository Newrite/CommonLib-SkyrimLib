#![allow(non_camel_case_types)]

// C++ `RE::ahkpWorld`
// Same-name header exists, but this dependency is currently only needed as an
// opaque pointer target from Havok/Bethesda wrapper types.
crate::core_util::abstract_type! {
    pub type ahkpWorld;
}
