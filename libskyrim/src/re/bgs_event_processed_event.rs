// Forward-declared C++ `RE::BGSEventProcessedEvent`.
//
// `ScriptEventSourceHolder.h` and `SkyrimVM.h` use this only as an event
// payload type parameter; CommonLibVR does not expose a same-name definition.
core_util::abstract_type! { pub type BGSEventProcessedEvent; }
