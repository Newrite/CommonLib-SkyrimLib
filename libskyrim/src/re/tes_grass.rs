// TODO: SOURCE - replace this pointer-only stand-in with a fuller translation
// from `RE/T/TESGrass.h` when TES grass layout or virtual access is needed;
// `TESLandTexture` currently only stores `TESGrass*` nodes inside `GNAM`.
core_util::abstract_type! {
    pub type TESGrass;
}
