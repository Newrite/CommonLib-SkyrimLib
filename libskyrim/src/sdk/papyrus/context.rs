//! High-level Papyrus callback context types.
//!
//! This module is the SDK-facing home for the existing user-facing callback
//! context layer, while the ABI glue remains in `libskyrim::skse::papyrus`.

pub use crate::skse::papyrus::{Context, LatentContext};
